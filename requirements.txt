# Arctic Shadow Tracker - Production Requirements
# Core dependencies for AIS vessel tracking and mapping

pyyaml>=6.0
requests>=2.28.0
pandas>=1.5.0
folium>=0.14.0
flask>=2.3.0
gunicorn==21.2.0

# Satellite imagery (Sentinel Hub API)
sentinelhub>=3.11.0
numpy>=1.24.0
# Pillow + tifffile power the SAR GeoTIFF → PNG thumbnail pipeline in
# scripts/collect_satellite.py. Both are transitive deps of sentinelhub, but
# pinned explicitly because our code imports them directly.
Pillow>=10.0.0
tifffile>=2024.1.0
# scipy.ndimage powers the vectorized CFAR ship detector in
# scripts/detect_vessels_cfar.py (median_filter, label, find_objects).
scipy>=1.11.0
# scikit-image regionprops for shape filtering in CFAR blob analysis.
scikit-image>=0.21.0
# shapely for land-mask point-in-polygon filtering of CFAR detections.
# Transitive dep of sentinelhub but imported directly in detect_vessels_cfar.py.
shapely>=2.0.0
